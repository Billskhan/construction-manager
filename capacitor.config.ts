import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yourcompany.construction',
  appName: 'construction-manager',
  webDir: 'dist'
};

export default config;
